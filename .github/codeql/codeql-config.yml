name: "certs CodeQL config"

# Exclude code that is not part of the published runtime from analysis.
# These paths generate only false-positive noise:
#   - tests exercise edge cases and use throwaway fixtures
#   - examples are illustrative integration snippets, not shipped code
paths-ignore:
    - test
    - '**/test/**'
    - examples
